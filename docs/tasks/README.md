# docs/tasks — TÀI LIỆU CŨ (đã bị thay thế)

Các file `agent_01..agent_05_*.md` trong thư mục này là **kế hoạch cũ** (chia 5 subagent: UI/UX, Admin ForgeControl, Supabase, 3D CAD engine, Orchestrator). Một phần nội dung đã được thi công (ví dụ "Open Walled Garden": `handleProtectedAction` trong `HomeView.tsx:128-131` hiện chỉ là hàm rỗng) và phần còn lại **không còn là nguồn sự thật**.

**Nguồn sự thật hiện tại:** `docs/README.md` → `docs/architecture/`, `docs/pages/`, `docs/database/`, `docs/security/`, `docs/SETUP_RUNBOOK.md`. Bộ kế hoạch cũ `docs/plans/01`–`07` **đã archive** (xem `docs/archive/README.md`); theo dõi việc còn lại ở `docs/tasks/UI-REFACTOR-NEXT-STEPS.md`.
Khác biệt căn bản: kế hoạch cũ mô tả mong muốn; kế hoạch mới dựa trên **số đo thật của source** (kèm `file:line`), có thứ tự phase, gate kiểm tra và tiêu chí nghiệm thu đo được.

Giữ lại các file này chỉ để tham chiếu lịch sử — không dùng để triển khai.
